---
title: "第 10 章 分类与比例数据"
maturity: sprout
updated: 2026-07-22
code-tools: false
---

```{=html}
<style>
.ch10-fig {
  background: #fff;
  border: 2px solid #1a1a1a;
  border-radius: 16px 6px 16px 6px;
  box-shadow: 5px 5px 0 #1a1a1a;
  padding: 1.6rem 1.4rem 1.4rem;
  margin: 0 0 2.5rem 0;
}
.ch10-fig-h {
  font-family: "Gochi Hand", "Caveat", "楷体", cursive;
  font-size: 1.2rem; font-weight: 700; color: #1a1a1a;
  text-align: center; margin-bottom: 1.2rem; letter-spacing: 0.1em;
}
.ch10-pipeline {
  display: grid;
  grid-template-columns: 1fr auto 1fr auto 1fr;
  align-items: center; gap: 0.5rem; margin: 0.4rem 0;
}
.ch10-stage {
  padding: 0.9rem 0.9rem 1rem; border-radius: 6px 16px 4px 18px;
  box-shadow: 4px 4px 0 #1a1a1a;
}
.ch10-stage.s1 { background: #ffebee; border: 2.5px solid #ef5350; transform: rotate(-1.5deg); }
.ch10-stage.s2 { background: #e1f5fe; border: 2.5px solid #0277bd; transform: rotate(0.5deg); }
.ch10-stage.s3 { background: #e8f5e9; border: 2.5px solid #2e7d32; transform: rotate(1deg); }
.ch10-stage-h {
  font-family: "Gochi Hand", "Caveat", "楷体", cursive;
  font-weight: 700; font-size: 0.95rem; margin-bottom: 0.4rem; text-align: center;
}
.ch10-stage.s1 .ch10-stage-h { color: #c62828; }
.ch10-stage.s2 .ch10-stage-h { color: #01579b; }
.ch10-stage.s3 .ch10-stage-h { color: #1b5e20; }
.ch10-stage-body {
  font-family: "楷体", "STKaiti", "Microsoft YaHei", cursive;
  font-size: 0.85rem; color: #333; line-height: 1.65; text-align: center;
}
.ch10-arrow {
  font-family: "Gochi Hand", "Caveat", cursive;
  font-size: 2.2rem; font-weight: 700; color: #1a1a1a;
  transform: rotate(-3deg); text-align: center; line-height: 1;
}
.ch10-caveat {
  margin-top: 1.1rem; padding: 0.6rem 0.85rem;
  background: #fafafa; border-left: 3px solid #1a1a1a; border-radius: 2px;
  font-size: 0.86rem; color: #1a1a1a; line-height: 1.7; text-align: center;
}
@media (max-width: 720px) {
  .ch10-pipeline { grid-template-columns: 1fr; gap: 0.8rem; }
  .ch10-arrow { transform: rotate(87deg); }
  .ch10-stage { transform: rotate(0deg); }
}
</style>

<div class="ch10-fig sketch">
  <div class="ch10-fig-h">一个百分数 → 找回分子分母与聚集 → 解释真实风险</div>
  <div class="ch10-pipeline">
    <div class="ch10-stage s1">
      <div class="ch10-stage-h">① 定义事件</div>
      <div class="ch10-stage-body">"受害"的判定标准是什么？类别必须互斥且可复核</div>
    </div>
    <div class="ch10-arrow" aria-hidden="true">→</div>
    <div class="ch10-stage s2">
      <div class="ch10-stage-h">② 保留分母</div>
      <div class="ch10-stage-body">3/5 和 60/100 都是 60%，但证据强度差几十倍</div>
    </div>
    <div class="ch10-arrow" aria-hidden="true">→</div>
    <div class="ch10-stage s3">
      <div class="ch10-stage-h">③ 沟通风险</div>
      <div class="ch10-stage-body">风险差、风险比、优势比：三种效应量回答不同问题</div>
    </div>
  </div>
  <div class="ch10-caveat">
    黑豹提醒：优势比不是风险比：当基线风险不低时，优势比会严重夸大效应。
  </div>
</div>
```

## 10.1 分类结果也来自明确的观测单位

前面两章处理的是连续响应变量（虫口密度、开花比例）。但保护研究中大量结果是分类的：树木是否受害、动物是否被检出、幼苗是否成活。分类数据的分析有自己的一套逻辑：起点仍然是观测单位。

本章的主项目回到森林病虫害试验。我们关心的不是虫口密度的均值，而是：综合防治是否降低了树木受害（damage_class ≥ 3）的比例？

在开始分析之前，先确认：
- 事件定义："受害" = damage_class ≥ 3（明确阈值，可复核）
- 观测单位：树木（但树木嵌套于小区：聚集问题见 10.6 节）
- 分母：每个处理小区的树木总数

```r
forest <- read.csv("../data/teaching/forest_pest_trial_long.csv",
                   stringsAsFactors = FALSE)

# 第三次调查
f3 <- forest[forest$visit == 3, ]

# 定义受害事件
f3$damaged <- f3$damage_class >= 3

# 先看小区层级的受害比例
plot_damage <- aggregate(
  damaged ~ block_id + plot_id + treatment,
  data = f3,
  FUN = function(x) c(n = length(x), damaged = sum(x))
)
plot_damage$n <- plot_damage$damaged[, "n"]
plot_damage$damaged_count <- plot_damage$damaged[, "damaged"]
plot_damage$damaged <- NULL
plot_damage$prop <- plot_damage$damaged_count / plot_damage$n
head(plot_damage)
```

<div class="chapter-route" aria-label="本章学习路线">
  <div>定义事件<span>互斥且可复核</span></div>
  <div>保留分母<span>分子和分母同等重要</span></div>
  <div>检查独立<span>卡方与 Fisher</span></div>
  <div>估计效应<span>风险差/风险比/优势比</span></div>
  <div>沟通风险<span>基线 + 绝对差</span></div>
</div>

::: {.learning-goals}
学完本章后，你能够：

1. 定义互斥且可复核的分类结果，并正确识别分子和分母。
2. 在卡方检验和 Fisher 精确检验之间做出有依据的选择。
3. 区分风险差、风险比和优势比，并解释为什么优势比不等于风险比。
4. 识别聚集数据中的伪重复问题：树木嵌套于小区。
5. 在报告风险时同时给出基线风险和绝对差异。
:::

## 10.2 比例必须同时保留分子和分母

"受害率 60%"。这个数字丢失了最关键的信息：分母是多少？

- 3/5 = 60%：只有 5 棵树，随机波动极大
- 60/100 = 60%：100 棵树，估计相对稳定
- 600/1000 = 60%：1000 棵树，估计很精确

同样的比例，完全不同的证据强度。永远不要只报告比例而不报告分子和分母。

```r
# 演示：相同比例，不同信息量
# 这三个"60%"的证据强度完全不同
cat("3/5 =", round(100 * 3/5), "%  ：95% CI: [",
    round(binom.test(3, 5)$conf.int[1] * 100), "%, ",
    round(binom.test(3, 5)$conf.int[2] * 100), "%]\n")
cat("60/100 =", round(100 * 60/100), "%  ：95% CI: [",
    round(binom.test(60, 100)$conf.int[1] * 100), "%, ",
    round(binom.test(60, 100)$conf.int[2] * 100), "%]\n")
cat("600/1000 =", round(100 * 600/1000), "%  ：95% CI: [",
    round(binom.test(600, 1000)$conf.int[1] * 100), "%, ",
    round(binom.test(600, 1000)$conf.int[2] * 100), "%]\n")
```

## 10.3 列联表回答的是哪一种独立性问题

列联表（contingency table）交叉分类两个分类变量。对于森林防治数据，我们关心：处理（对照/综合防治）和受害状态（是/否）是否独立？

| | 受害 | 未受害 | 合计 |
|---|---|---|---|
| 对照 | a | b | a+b |
| 综合防治 | c | d | c+d |
| 合计 | a+c | b+d | n |

零假设：处理与受害状态独立：即受害比例在处理间无差异。

但注意：这个表里的计数应该以小区为单位还是以树木为单位？如果以树木为单位（a = 对照组受害树木数），我们假设了每棵树是独立的。这违反了树木嵌套于小区的设计。正确做法是先在小区层级汇总（见 10.6 节）。

## 10.4 卡方与 Fisher 方法的条件

卡方检验（χ² test）：基于大样本正态近似。适用条件：期望频数 ≥ 5（传统标准）。当期望频数太小时，卡方近似不可靠。

<details class="why-derivation">
<summary>为什么 χ² 统计量是 Σ(观察−期望)²/期望？</summary>

**单格视角：一个格子的偏差。** 只看表格里一个格子：$H_0$ 下观测数 $O$ 服从二项分布 $\mathrm{Binomial}(n, p)$，期望是 $E = np$。偏差 $d = O - E$ 平均为零，但具体某次抽样里可正可负。

**为什么分母是期望而不是别的？** 计数数据有一个特征：方差约等于期望。对二项分布，$\mathrm{Var}(O) = np(1-p) \approx np = E$（当 $p$ 不太大时）；对泊松分布，$\mathrm{Var}(O) = E$ 精确成立。所以 $\frac{(O-E)^2}{E}$ 是"偏差平方除以自身方差"——和 $Z^2 = \frac{(\bar{x}-\mu)^2}{\mathrm{Var}(\bar{x})}$ 是同一个思想：用方差做标尺，把不同量级的格子拉到同一尺度。期望 5 的格子偏差 1，和期望 200 的格子偏差 1，证据分量完全不同；除以期望后两者才可比。

**把 Z 平方：正态 → 卡方。** 大样本下 $Z = \frac{O-E}{\sqrt{E}} \approx N(0,1)$（中心极限定理），所以 $Z^2 \approx \chi^2_1$。每个格子贡献一个这样的标准化平方，全部加起来：

$$\chi^2 = \sum_{\text{所有格子}} \frac{(O_{ij} - E_{ij})^2}{E_{ij}}$$

**为什么 4 个格子只有 1 个自由度？** 这是 χ² 最容易被误解的地方。2×2 表有 4 个格子，但行、列边际合计被数据钉死（比如 $O_{11}+O_{12}=n_1$）。边际固定后，4 个偏差 $(O_{ij}-E_{ij})$ 只由一个自由量 $d = O_{11} - E_{11}$ 决定，其余三个恰好是 $-d, -d, +d$（期望值也满足同样的边际约束）。所以：

$$\chi^2 = d^2\Big(\frac{1}{E_{11}} + \frac{1}{E_{12}} + \frac{1}{E_{21}} + \frac{1}{E_{22}}\Big)$$

一个自由量平方，配一个 $\chi^2_1$ 分布。推广到 $r \times c$ 表：$(r-1)(c-1)$ 就是"不被边际约束钉死的格子数"——这是自由度主线在分类数据中的第三次出现（第 5 章：$n-1$；第 9 章：$k-1$ 与 $N-k$）。

**期望频数 ≥ 5 为什么是门槛？** 分母 $E$ 太小（比如 2）时，分子偏差稍微大一点，$\frac{(O-E)^2}{E}$ 就剧烈跳动，正态近似失效。门槛保证每个格子的贡献稳定，总和才接近真正的 $\chi^2$ 分布。

::: {.key-judgment}
判断：χ² 的形状回答"偏差有多大"，自由度回答"有多少个自由偏差"。两者分开记：前者来自"方差≈期望"的计数特征，后者来自边际约束。
:::
</details>

Fisher 精确检验：基于超几何分布，不依赖大样本近似。适用于小样本或稀疏表格。但 Fisher 检验假设行列边际固定。这在某些设计下不完全合理。

```r
# 先在小区层级汇总：以小区是否超过受害阈值为分类
# 定义：小区受害率 > 30% 为"高受害"
plot_damage$high_damage <- plot_damage$prop > 0.3

# 只取对照和综合防治
pd2 <- plot_damage[plot_damage$treatment %in% c("control", "integrated"), ]

# 列联表
tbl <- table(pd2$treatment, pd2$high_damage)
print(tbl)

# 卡方检验
chisq.test(tbl)

# Fisher 精确检验（适用于小样本）
fisher.test(tbl)
```

## 10.5 风险差、风险比与优势比

三种效应量回答不同的问题：

风险差（risk difference, RD）：对照风险 − 处理风险。最直观："综合防治使受害率降低了 X 个百分点"。单位是百分点。

风险比（risk ratio, RR）：处理风险 / 对照风险。"综合防治组的受害风险是对照组的 X 倍"。

优势比（odds ratio, OR）：处理组的优势 / 对照组的优势。优势 = 受害概率 / (1 − 受害概率)。OR 在数学上方便（与逻辑回归直接对应），但在解释上容易误导：当基线风险不低时，OR 会严重偏离 RR。

```r
# 计算三种效应量
# 从列联表
a <- tbl["control", "TRUE"]    # 对照高受害
b <- tbl["control", "FALSE"]   # 对照低受害
c <- tbl["integrated", "TRUE"] # 综合防治高受害
d <- tbl["integrated", "FALSE"]# 综合防治低受害

# 风险
risk_ctrl <- a / (a + b)
risk_int <- c / (c + d)

# 风险差
rd <- risk_ctrl - risk_int
cat("风险差 (RD):", round(rd * 100, 1), "个百分点\n")

# 风险比
rr <- risk_int / risk_ctrl
cat("风险比 (RR):", round(rr, 2), "\n")

# 优势比
odds_ctrl <- risk_ctrl / (1 - risk_ctrl)
odds_int <- risk_int / (1 - risk_int)
or <- odds_int / odds_ctrl
cat("优势比 (OR):", round(or, 2), "\n")

cat("\n注意：当基线风险为", round(risk_ctrl * 100), "%时，\n")
cat("OR =", round(or, 2), "不等于 RR =", round(rr, 2), "\n")
```

<div class="mascot-note">
<img src="../images/mascot/cat-point.svg" alt="" class="mascot" aria-hidden="true">
<div class="mascot-note-copy">
黑豹提醒：优势比不是风险比
<p>当基线风险为 50% 时，OR = 2.0 对应的 RR 只有约 1.33。报告"风险降低 50%"（OR = 0.5）而实际绝对风险只从 50% 降到 33%。这在保护沟通中可能严重误导管理决策。</p>
</div>
</div>

:::: {.classic-story}
<div class="classic-story-label">古事，今问</div>
<div class="classic-story-title">朝三暮四</div>

《庄子·齐物论》写养猴人分橡实，先说早上三份、晚上四份，群猴发怒；改成早上四份、晚上三份，群猴便高兴。庄子说：“名实未亏，而喜怒为用。”[^three-four]

<div class="classic-story-judgment"><span>统计判断</span> 同一张列联表可以写成风险差、风险比或优势比。底层计数没有变化，读者感受到的效果却可能不同。报告比例时必须交代分子、分母和基线风险，不能只挑听起来最大的数字。</div>

<div class="classic-story-boundary"><span>类比边界</span> 早晨和晚上的食物未必等价，饥饿和时间偏好都可能让两种分配产生真实差别。风险差、风险比和优势比又在什么情况下回答了不同问题，而不只是换一种说法？</div>
::::

[^three-four]: 庄周，《庄子·齐物论》。原文参见[中国哲学书电子化计划](https://ctext.org/dictionary.pl?char=%E7%8B%99%E5%85%AC&if=gb)。

## 10.6 聚集、稀疏与完全分离

分类数据在保护研究中有两个特殊问题：

聚集（clustering）：树木嵌套于小区。同一小区的树木共享处理和环境，不是独立的。把树木汇总成 2×2 表（忽略小区）等于制造伪重复。

完全分离（complete separation）：当某个处理的观测全部是同一结果时（如综合防治所有小区都是低受害），标准 logistic 回归无法拟合。这是第 12 章的接口。

```r
# 反例：树级汇总（忽略小区聚集）
tree_tbl <- table(f3$treatment[f3$treatment %in% c("control", "integrated")],
                  f3$damaged[f3$treatment %in% c("control", "integrated")])
print(tree_tbl)
cat("树级卡方 P =", round(chisq.test(tree_tbl)$p.value, 4), "\n")

# 正确做法：小区级汇总
cat("小区级 Fisher P =", round(fisher.test(tbl)$p.value, 4), "\n")

cat("\n树级分析把 400 棵树当作独立观测：\n")
cat("实际上只有 20 个独立实验单位（小区）。\n")
cat("树级 P 值通常更小，不是因为效应更强，而是因为伪重复。\n")
```

## 10.7 从比例证据到风险沟通

完整的风险沟通必须包含：
1. 基线风险：对照组受害率是多少？
2. 绝对风险降低：综合防治使受害率降低了多少个百分点？
3. 相对风险降低：降低的比例是多少？
4. 不确定性。这些估计的置信区间？
5. 聚集提醒：分析在小区层级进行，树木不是独立重复。

```r
cat("=== 风险沟通摘要 ===\n\n")
cat("对照组高受害小区比例:", round(risk_ctrl * 100), "%\n")
cat("综合防治组高受害小区比例:", round(risk_int * 100), "%\n")
cat("绝对风险降低:", round(rd * 100), "个百分点\n")
cat("相对风险:", round(rr, 2), "\n")
cat("\n注意：分析在小区层级（n = 20 个小区），不是树木层级。\n")
cat("未纳入分析：非目标影响、防治成本、实施完整性。\n")
```

## 本章小结

- 比例必须保留分子和分母。3/5 和 60/100 都是 60%，但证据强度完全不同。
- 卡方检验依赖大样本近似；Fisher 检验适用于小样本。选择取决于期望频数，不是"哪个 P 值更小"。
- 优势比不是风险比。当基线风险不低时，OR 会严重偏离 RR：在保护沟通中优先使用风险差和风险比。
- 树木嵌套于小区：树级汇总制造伪重复。分类分析同样需要尊重设计层级。
- 风险沟通必须同时给出基线风险和绝对差异。只报告"风险降低 50%"而不说基线风险是 2% 还是 50%，是误导。

## 练习

1. 识别层。以下比例的分母是什么？证据强度如何？
   (a) "发芽率 80%"（20 粒种子，16 粒发芽）
   (b) "检出率 45%"（60 个样点，27 个至少一次检出）

2. 解释层。为什么优势比不等于风险比？在什么情况下两者接近？

3. 应用层。用森林数据比较标准防治和生物防治的"高受害"比例。使用小区级分析，报告风险差和风险比。

4. 诊断层。树级卡方检验 P = 0.001，小区级 Fisher 检验 P = 0.08。为什么差异这么大？哪个更可信？

5. 综合层。把相机检出数据（detected）写成二项结构：分子是什么？分母是什么？独立单位是什么？为什么不能把 1080 行当作独立二项试验？

## 继续学习

→ [第 11 章：相关与线性回归](11-correlation-regression.qmd)
